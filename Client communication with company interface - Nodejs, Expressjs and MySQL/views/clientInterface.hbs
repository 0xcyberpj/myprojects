<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Welcome</h1>
    <h3>Enter your ID</h3>
    <form action="./clientid_login" method="get">
      <input type="number" name="clientID" value="" placeholder="ClientID">
      <input type="submit" name="" value="G0">
      {{#if message}}
      <h3>{{message}}</h3> {{!--there should be no space in if message and closing of if--}}
      {{/if}}
      {{#if message2}}
      <h3>{{message2}}</h3> {{!--there should be no space in if message and closing of if--}}
      {{/if}}
      {{#if message3}}
      <h3>{{message3}}</h3> {{!--there should be no space in if message and closing of if--}}
      {{/if}}
    </form>

    <br><br><br><br><br>

    <h3>Send Message to Constructor</h3>
    <form action="./SendMessage" method="get">
      <input type="number" name="clientID" value="" placeholder="clientID">
      <input type="text" name="SendMessage" value="" placeholder="Message">
      <input type="submit" name="" value="Send Message">
      {{#if message1}}
      <h4>{{message1}}</h4> {{!--there should be no space in if message and closing of if--}}
      {{/if}}
    </form>
  </body>
</html>
